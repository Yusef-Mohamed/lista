import Logo from "./Logo";

const Footer = () => {
  return (
    <footer className="px-4 py-8 bg-muted grid grid-cols-2">
      <div>
        <Logo />
      </div>
      <div>ss</div>
    </footer>
  );
};

export default Footer;
